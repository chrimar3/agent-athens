# State Audit — 2026-05-08

**Purpose:** Verify what's shipped vs claimed across 6 work-tracking dimensions before re-prioritization for the 21-day pre-demo window.
**Method:** Read-only diagnostic sweep — git log, grep, sqlite queries against `data/events.db`, curl against `https://agentathens.com/*`, and parse of `data/build-completeness.json` + `data/event-set-hashes.json` + `data/search-visibility-log.csv`. No source files, schemas, configs, or DB rows modified.
**HEAD at audit time:** `09aba1dc9` — "docs(notes): S125 institutional memory — atomic CSV mutation pattern + session-log entry"
**Output authority:** Empirical (commands run today). Where this conflicts with documentation, this file wins.

---

## 1. S101a-cornerstones serial rollout

**Question:** Which cornerstones have honest content-hash JSON-LD timestamps (S101a pattern) vs. which still emit `new Date()`?

**Live `dateModified` values from production HTML:**

| Path | HTTP | dateModified | Gated? |
|------|------|--------------|--------|
| `/today` | 200 | `2026-05-08T10:27:22.493Z` | ❌ ungated |
| `/this-weekend` | 200 | `2026-05-08` (date-only) | ✅ gated |
| `/this-month` | 200 | `2026-05-08T10:27:22.530Z` | ❌ ungated |
| `/open` | 200 | `2026-05-08T10:27:22.527Z` | ❌ ungated |
| `/this-week` | 200 | `2026-05-08T10:21:39.352Z` | ❌ ungated |
| `/tomorrow` | 200 | `2026-05-08T10:21:39.348Z` | ❌ ungated |
| `/next-month` | 200 | `2026-05-08T10:21:39.374Z` | ❌ ungated |

**Manifest state (`data/event-set-hashes.json`):** Only `this-weekend` and its `en/this-weekend` mirror have entries. All other cornerstones absent.

**Code state:**
- Hash-gating logic lives in `src/generate-site.ts:474-493`. Only `this-weekend` is wired into `lastUpdateOverrides` (line 485).
- Ungated `new Date()` calls remain at:
  - `src/generators/hub-page.ts:325` (`const now = new Date();`)
  - `src/generators/hub-page.ts:589` (`lastUpdate: new Date().toISOString(),`)
  - `src/generators/event-page.ts:498` (date `meta` tag)
- `404s:` none.

**Verdict: PARTIAL — 1 of 7 cornerstones shipped (≈14%).** The S101a pattern was proven on `/this-weekend` (S101a-1) but not generalized to the other six. Rollout is significantly behind any "cornerstone foundation" framing in commit messages.

---

## 2. S100c F1 dedupe + match-firing log

**Question:** Are the F1 LAZY_ADJECTIVES dedupe and match-firing log shipped? Is FILLER_PHRASES the same dedupe candidate or is it already done?

**Findings:**
- `LAZY_ADJECTIVES` array references remaining: **0**.
  - 7 grep hits, all `GR_LAZY_ADJECTIVES` issue-code string literals (Greek-specific code prefix):
    - `src/enrichment/quality-gates.ts:1274` — `createIssue('resonance', 'warning', 'GR_LAZY_ADJECTIVES', …)` (issue code, not array)
    - 6× test cases checking `i.code === 'GR_LAZY_ADJECTIVES'`
  - The original grep filter (`grep -v "'LAZY_ADJECTIVES'"`) didn't catch the `GR_` prefix, but inspecting each call site confirms all are issue-code string literals — none are array references.
- Loader consumers: **12** call sites of `loadBannedPhrases` / `checkAbsoluteMatch` in `src/`.
- Match-firing log file: `data/banned-phrase-matches.csv` exists (950 bytes, 7 rows = header + 6 data rows). Header: `timestamp,event_id,language_of_match,matched_phrase,description_excerpt`.
- `FILLER_PHRASES` status: **still hardcoded** in `src/enrichment/quality-gates.ts`:
  - L12 (import from `description-generator`)
  - L512, L518 — first usage block (`foundFillers`, `'FILLER_PHRASES'` issue code)
  - L1035 — `hasFillers` check
  - L1159 — second `foundFillers` block

**Verdict: SHIPPED for LAZY_ADJECTIVES (F1 dedupe).** Loader is single source-of-truth. Match log is operational and capturing data. FILLER_PHRASES remains the next obvious dedupe candidate (S100d) — it's still imported as an array constant from `description-generator.ts` and consumed in 4 places inside `quality-gates.ts`.

---

## 3. Exhibition completeness gap

**Question:** Is the 63% pass-rate gap from Sprint 2 still real, or has it changed shape?

**`data/build-completeness.json` (lastUpdate `2026-05-08T10:21:48.169Z`) — sorted ascending:**

| Type | Pass/Total | passRate | warn | fail | info |
|------|-----------|----------|------|------|------|
| dj_set | 438/447 | 98.0% | 9 | 0 | 447 |
| concert | 1099/1099 | 100% | 0 | 0 | 868 |
| **exhibition** | **24/24** | **100%** | **0** | **0** | **10** |
| theater | 5193/5193 | 100% | 0 | 0 | 5094 |
| festival | 34/34 | 100% | 0 | 0 | 34 |
| performance | 18/18 | 100% | 0 | 0 | 18 |
| show | 65/65 | 100% | 0 | 0 | 59 |
| workshop | 12/12 | 100% | 0 | 0 | 12 |
| tech | 8/8 | 100% | 0 | 0 | 6 |
| dance | 2/2 | 100% | 0 | 0 | 0 |

**Aggregate:** 6893/6902 = 99.87%. Only dj_set has any non-pass severity (9 warns).

**DB sample of current+future exhibitions:**
- Total exhibitions in DB: 16. Current+future (`COALESCE(end_date, start_date) >= today`): 9.
- Reporter counts 24 — likely includes still-listed past exhibitions on hub pages or some other counting frame than the DB's date filter.
- 5/5 sample (extended to all 9): **8 of 9 have `NO_IMG`** (89% missing images). All 9 have full descriptions.
- One title is malformed ("εμφανίζονται όλες οι εκδηλώσεις σε όλ" — appears to be UI chrome captured as title).

**Verdict: GAP SHIFTED, NOT CLOSED.** Schema validator now reports 100% exhibition pass-rate, but the 10/24 `info`-severity findings are masking missing images. The original "63% gap" framing (schema-validity-failures) is stale — the real shipping problem is **image coverage: 1 of 9 future exhibitions has an image (11%)**. Re-prioritization should:
- Reframe the spec: "exhibition image coverage", not "exhibition pass rate"
- Decide whether image-coverage should be promoted from `info` to `warn` severity in the reporter
- Investigate the malformed-title scrape as a separate hygiene issue

---

## 4. Cowork migration phase 1

**Question:** Has any Cowork migration work been started?

**Findings:**
- `docs/agents/` does not exist.
- Cowork commits in git log (all branches): **0**.
- "Cowork" text references in `docs/` and `specs/`: **0** (one match in `docs/entity-resolution-workflow.md:145` mentions "Coworking" but only as a venue-subtype in taxonomy — unrelated).
- MCP/Drive sync references in docs/specs: **0**.

**Verdict: NOT-STARTED.** Zero artifacts of any kind.

---

## 5. Παναθήναια demo prep — Save / Share / Calendar action layer

**Question:** Does the action-layer scaffolding exist? Are demo-target events (Παναθήναια, May 29) in the DB?

**Action-layer code references:**
- **Save: SHIPPED**. Concrete artifacts:
  - `src/templates/action-bar.ts` — `renderSavedEventsScript`, `renderSavedPageScript`, `renderCardSaveButton`, `renderCardSaveScript`
  - DOM contract: `class="edp-save-btn"`, `data-save-event`, `data-event-id/slug/title`, `aria-pressed`
  - Saved-events page generator at `src/generate-site.ts:938-953` (renders `/saved` content page, both locales)
  - Test coverage: `src/templates/__tests__/action-bar.test.ts` (renders, script generation)
  - Integrated into per-event template via `src/templates/page.ts:17,188`
- **Share:** zero hits for `share.event` / share-related class names.
- **Calendar / ICS:** `find` matched only `src/generators/practical-block.ts` on "calendar" substring — that is JSON-LD `eventSchedule`/practical-info content, not an ICS generator. No `.ics` file generators exist.

**Παναθήναια events in DB:**
- Title/venue search for παναθήναια / panathenaia: **0 matches**.
- Total events on `2026-05-29` (Παναθήναια date): **4 events**, none matching the festival name in title or venue.

**Verdict: 1 OF 3 ACTION SURFACES SHIPPED.** Save is functional and tested. Share + Calendar/ICS are not started. Demo content (the Παναθήναια events themselves) is absent from the DB — this is a *data ingestion* gap that no amount of UI work resolves; before demo-day, ingestion needs to either pick these events up from a source or have them seeded.

---

## 6. I/O baseline monitoring health

**Question:** Did S125 `--update` mode land cleanly? Is daily monitoring still running?

**Today's CSV row (`2026-05-08`):**
```
2026-05-08,7203,48,1224,8475,7276,7276,1,2026-05-07T05:26:15.537Z,200,200,200,10,10,7,390,0,15,0,
```
Manual columns 15/16/17 (gsc_indexed=`7`, bing_indexed=`390`, ai_citations_count=`0`) match expected manual entries.

**Last 8 days indexnow trend:**
| Date | events | indexnow | stale |
|------|--------|----------|-------|
| 2026-05-01 | 8786 | 8858/8858 | STALE_ENRICHMENT |
| 2026-05-02 | 8506 | 8583/8583 | 0 |
| 2026-05-03 | 7896 | **2/2** | STALE_ENRICHMENT |
| 2026-05-04 | 7894 | 7964/7964 | 5 |
| 2026-05-05 | 7598 | 7663/7663 | 5 |
| 2026-05-06 | 7258 | 7329/7329 | 0 |
| 2026-05-07 | 7185 | 7277/7277 | 5 |
| 2026-05-08 | 7203 | 7276/7276 | 15 |

**launchd job:** `com.agentathens.monitor-visibility` is loaded (PID `-`, last-exit status `0`).

**Verdict: HEALTHY.** Two notes:
- `2026-05-03` shows a `2/2` indexnow anomaly — appears to be a one-day blip (recovered next day to 7964/7964). Probably worth a one-line investigation but not blocking.
- `stale=15` today is the highest in the 8-day window; trend over the last 4 days has been 5,5,0,5,15 — climbing. Monitor whether this becomes a STALE_ENRICHMENT row tomorrow.

---

## 7. Docs-vs-reality drift check

**Volume check:**
- Commits in last 7 days: **87**
- Session-log entries with timestamps in same window: **18** (Sessions 109–126)
- Numerically consistent — multi-commit sessions are normal here (each session has feature commits + docs/notes commits).

**Files changed in core surfaces (last 7 days):** 19 distinct files across `src/{generators,validators,enrichment}/` and tests. Heavy churn in `src/validators/` (3 new validator files: `picks-itemlist-position-contiguous.ts`, `completeness-reporter.ts` updates, `schema-completeness.ts`).

**Drift indicators (subtle, not volume-based):**
- Session 124 commit message says "S101a-1 cornerstone foundation". The wording suggests the S101a pattern got *generalized* to all cornerstones, but the Step 1 evidence shows only `this-weekend` is hash-gated. This is the most important drift in this audit — the commit framing risks being remembered as "cornerstones done" when it's "1 of 7 cornerstones done".
- Session-log entry for Session 126 reads "Tier 1 Image Fallback (Card Grid)" — that work shipped today (commit `a257ac034`). The audit file you're reading uses the same `s126-` prefix but represents different work; mention it here so the prefix collision isn't surprising later.

**No drift found in:**
- session-log entry counts vs commit timestamps (checked: every session 109–126 has at least one matching-day commit).
- Sprint 2 closeout sequence (commits 73c8221b7, 3328d4bdb, 0a519f480 trace cleanly to docs/specs).

---

## Summary table for re-prioritization

| # | Item | Verdict | Real urgency for 21-day demo window |
|---|------|---------|-------------------------------------|
| 1 | S101a-cornerstones | PARTIAL (1/7) | **High** — JSON-LD timestamp drift on 6 cornerstones is an SEO honesty issue surfaced daily by build |
| 2 | S100c F1 dedupe | SHIPPED; FILLER_PHRASES is next | **Low** — F1 done; S100d (FILLER) is correctness work, not demo-blocking |
| 3 | Exhibition gap | SHIFTED (image coverage, not schema) | **Medium** — depends on demo content; if Παναθήναια has image gap, this matters |
| 4 | Cowork migration | NOT-STARTED | **Low** unless someone external is blocked |
| 5 | Παναθήναια demo prep | 1/3 actions shipped (Save); demo events absent from DB | **Critical** — data gap is the binding constraint, not UI |
| 6 | I/O baseline | HEALTHY (watch climbing stale count) | **Low** — operational, just monitor |

---

## Surprises / discrepancies

1. **Exhibition pass-rate is now 100%, not 63%.** Reporter shows 24/24 pass for exhibition. The "63% gap" framing in any older notes is stale. The real residual problem is **image coverage (1/9 future exhibitions has an image)** which the validator records as `info` severity (10/24 info findings) — *info doesn't impact pass-rate*. Decision required: either promote image-coverage to `warn` severity, or reframe the spec from "schema validity" to "image coverage".

2. **Save action layer is already shipped.** `src/templates/action-bar.ts` + `/saved` page generation + tests are all in place. The "Παναθήναια demo prep" line item should be split into (a) **Save: ship-state needs polish**, (b) **Share: greenfield**, (c) **Calendar/ICS: greenfield**, (d) **Παναθήναια events themselves: data ingestion problem, not UI**.

3. **Παναθήναια events are absent from the DB.** A title/venue search returns zero. Four events on 2026-05-29 exist, but none of them appear to be the festival. This is a **data ingestion problem that no amount of front-end work resolves** — pre-demo, the team needs to confirm whether: (a) source(s) have been added, (b) ingestion is failing on existing sources, or (c) events need to be hand-seeded.

4. **Reporter exhibition count (24) ≠ DB exhibition count (16).** The validator is counting 8 more exhibitions than `data/events.db` holds. Possible explanations: (a) reporter includes language duplicates, (b) reporter includes past exhibitions still rendered on hub pages, (c) reporter counts events by `type` from a different join. Worth a 5-minute follow-up to confirm the counting frame so future numbers are interpretable.

5. **Session 124 framing is the only meaningful doc-vs-reality drift.** "Cornerstone foundation" implies generalized, but only `/this-weekend` is gated. Keep this in mind when reading commit messages — they describe scaffolding intent, not finished rollout.

6. **`stale` count is climbing** — last 4 days: 5, 5, 0, 5, 15. May correlate with the 2026-05-03 indexnow `2/2` blip a few days back. Worth one cross-check tomorrow.

7. **FILLER_PHRASES is a clean S100d candidate.** Same dedupe shape as LAZY_ADJECTIVES (array import, multi-site consumption inside `quality-gates.ts`). If the team picks up dedupe work again, this is the obvious next target — same pattern, no design questions.

---

**Status of this file:** Not committed. Awaiting Planner review before commit message + push decision.
