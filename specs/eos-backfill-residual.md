# Εως backfill residual (S-F2a, 2026-06-12)

Migration: `scripts/migrate-eos-end-date.ts` · Parser: `src/utils/run-end-token.ts` · Tokens: `config/parsing-tokens.json`
Baseline HEAD at session start: `5d0ba1972 2026-06-12`. Backup: `data/events.db.bak-sf2a` (untracked, never staged).

## Result

| Population | Count | Disposition |
|---|---|---|
| Backfilled (exact-form `Εως YYYY-MM-DD` → end_date + description NULL) | **11,633** | done, in one transaction |
| `description=''` → NULL (global G5 normalization, operator-approved) | **2,828** | done, same transaction |
| Excluded: end<start | **0** | class empty |
| Excluded: non-ISO after token / prose variants / mid-string | **0** | classes empty (corpus was machine-generated by old `scrape-all.ts:607`) |
| Token-present-unparsed anomalies | **0** | `logs/eos-backfill-anomalies.log` untouched |
| Non-athinorama riders in the backfill | 14 (more.com 12, megaron 2) | rode the migration; their scrapers untouched — re-audit only if the pattern recurs in new rows |
| Εως mid-prose OUTSIDE the corpus (rows with end_date already set or full prose) | ~2 (onassis) | out of scope; genuine prose, not the defect shape |

Post-verify invariants (script-enforced): zero parseable run-end rows with empty end_date · zero `end_date < start_date` · zero `description=''`.

## Enrichment-queue delta (amendment 2 — true numbers for F2b / enrichment-forensic)

- Verified-unenriched queue (keys on `full_description_en`): **unchanged** — past 11,152 / upcoming 88, identical before and after. The migration created **zero** new active-throughput work.
- `description IS NULL` population after migration: past **13,827** / upcoming **634**. Per G5 these are nominally enrichment-eligible; the ~10,993 past Εως rows belong in the junk-queue category alongside the existing pending-past mass, NOT in any active throughput counter.

## Corrected architecture note (amendment 4 — repair planner reference docs from this, not memory)

The stale `src/scraping/` + `src/importers/` map is wrong for the daily pipeline. Actual layout:

- **Scraping orchestration + all daily scrapers**: `scripts/scrape-all.ts` — one file. `scrapeAthinorama()` (theater/music guides; now exports `parseTheaterDateRange` for tests), per-source functions for the other 9 active sources (`src/config/active-source-ids.ts` is the registry; `type SourceId` derives from it).
- **Write path / dedup for scraped rows**: `saveEvents()` in the same `scripts/scrape-all.ts` — `INSERT … ON CONFLICT(id) DO UPDATE` where `end_date = COALESCE($end_date, end_date)` and **description is never updated on conflict** (so re-scrapes can't resurrect Εως prose or null a backfilled end_date). Pre-write passes: scope filter (`shouldExcludeEvent`), categorizer (`categorizeEventSimple`), `normalizeDateField`, `normalizePriceType`, `normalizeGenres`. G5 bind: `$description: e.description || null`.
- **`src/db/database.ts` `upsertEvent()/eventToRow()`**: the *enrichment/newer* write path only — not the daily scrape route. (Its `eventToRow` still coerces description to `''` at line ~114; out of S-F2a scope, flag if enrichment writes are ever audited for G5.)
- **Standalone scraper modules** (`scripts/scrape-megaron.ts` etc.) exist for some sources and are imported by scrape-all; athinorama is NOT one of them.

## Boundary confirmation (for F2b)

`resolveEventStatus` (`src/utils/schema-geo.ts:99`) and lifecycle (`src/utils/event-lifecycle.ts`, pageable query `src/db/database.ts:508`) consult end_date **only for `type='exhibition'`** — so this backfill changes no status/lifecycle/sitemap behavior for the 10,869 theater rows. The 214-page EventCompleted population is now *data-ready* for F2b: when F2b extends run-aware lifecycle to theater, the end_dates are already in place. Emission was verified a no-op this session: endDate emits from end_date when present (`src/generators/event-page.ts:193-195`); description cascades and never emits `""` (`:163-165`).
