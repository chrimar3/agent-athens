# Dependencies
node_modules/
.bun/

# Environment
.env
.env.local

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Build artifacts (deployed via Netlify CLI, not git)
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Temporary files
temp/
tmp/

# Enrichment working directories
temp-descriptions/
temp-research/

# Downloaded event images (cached locally, served from dist/)
data/images/

# Runtime artifacts (not source code)
# These change every pipeline run and were causing 40-min git pushes (2026-04-08).
# Backed up locally via scripts/backup-events-db.sh (7-day rolling).
data/events.db
data/events.db.*
data/events.db-shm
data/events.db-wal
data/kpi.db
data/kpi.db.*
data/kpi.db-shm
data/kpi.db-wal
data/content-hashes.json
data/content-hash-snapshots/
temp-briefs/
data/health-reports/
data/search-visibility-log.csv
data/banned-phrase-matches.csv

# Local Netlify folder
.netlify
.git-old/

# Pipeline lock files (runtime state)
.pipeline-*.lock
.auto-enrich.lock
.auto-enrich.lock.d/

# Parsed email artifacts (not source code)
data/emails-to-parse/
