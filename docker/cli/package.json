{
  "name": "agent-athens-container-cli",
  "private": true,
  "description": "Command-line tools baked into the pipeline image (docker/Dockerfile). Installed with `npm ci` so every tarball is checked against package-lock.json integrity hashes.",
  "dependencies": {
    "@anthropic-ai/claude-code": "2.1.280",
    "bun": "1.3.11",
    "netlify-cli": "27.8.1"
  }
}
